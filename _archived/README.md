# _archived — retired surfaces (the story reads on disk, no git archaeology needed)

Retired 2026-08-01 in the solidification pass (Keemin-directed: every live file
must directly matter for the World; documentation at root, indexed).

- **`CALLS.md`** — the 30-call working log of the walk/water/sea build passes
  (C1–C30). Ground-truthed against live main 2026-07-31: the settled calls are
  law in the shipped code and the root README; still-open at archive time were
  **C10** (crossing-spans precondition sentence), **C13** (the clock seam
  bless/reject), and **C20** (the-town-centre-crossing dangling ref +
  feature-ref lint) — all held for Keemin, tracked on the operator lane, not
  lost with this file.
- **`RESULT.md`** — the water true-shape pass report (rings, the containment
  re-homing table, v0 gate-off). Its content merged to main long before
  archive; kept as the pass's record.
- **`sims/run-01`** — the 2026-07-17 behavioral sim (the goldmine run: sol's
  Heart House failure chain, the anchor bug, parcel-first lessons). Predates
  the 07-22 nesting ruling; was `world-poc.mjs`'s no-arg legacy fixture until
  this pass pointed the PoC at the real seeded tree. No test ever referenced
  it. The sim's lessons live in the shipped law (parcel-first, loud refusals,
  real anchors).
